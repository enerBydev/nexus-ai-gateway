# Build artifacts
/target/
**/*.rs.bk
*.pdb
*.bk
*.bak
*.backup

# Cargo lock for libraries (keep for binaries)
# Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db


# Logs
*.log
*.pid

# Test artifacts
/coverage/
/.coverage/

# Documentation build
AUDIT_REPORT_NIM_V4.md
/docs/
nexus-beacon-receiver/

# Dotfiles (specific — avoid blanket .* which blocks .github/, .cargo/, etc.)
.env
.env.*
*.env.local
.nexus-ai-gateway.env
.claude
.omc
.cargo/
.wrangler


# Beacon receiver microservice (developed in-tree, deployed to CF Workers separately)
/nexus-beacon-receiver/

# Repo identity
.repo

# Temporary files
/tmp/
*.tmp
*.temp

# Claude Code auto-generated (do not track)
CLAUDE.md
.gstack/

# Codex auto-generated (do not track)
AGENTS.md

# Local test/dev environment (entorno de pruebas aislado — setup NEXUS-094)
# CARGO_TARGET_DIR aislado para pruebas (el watcher nunca dispara)
/target-test/
# hooks locales: symlinks curados a scripts/hooks/ (tracked para persistir across clones)
# Requiere: git config core.hooksPath .githooks-local
# cache de checksums de go-task
.task/
# runtime local de orquestación
.omo/
# Nota: .env.test ya está cubierto por ".env.*" arriba

